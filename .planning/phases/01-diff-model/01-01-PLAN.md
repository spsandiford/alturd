---
phase: 01-diff-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - .gitattributes
  - internal/diff/model.go
  - internal/diff/testdata/simple.diff
  - internal/diff/testdata/binary.diff
  - internal/diff/testdata/rename.diff
  - internal/diff/testdata/mode-only.diff
  - internal/diff/testdata/submodule.diff
  - internal/diff/testdata/no-newline.diff
  - internal/diff/testdata/new-file.diff
  - internal/diff/testdata/deleted-file.diff
  - internal/diff/testdata/multi-file.diff
  - internal/diff/testdata/multi-hunk.diff
  - internal/diff/testdata/large-line.diff
  - internal/diff/testdata/many-tokens.diff
  - internal/diff/testdata/multiline-string.diff
autonomous: true
requirements: [DIFF-01, DIFF-05]
must_haves:
  truths:
    - "go build ./... succeeds with go-gitdiff, go-diff, and chroma/v2 resolved at locked versions"
    - "internal/diff package compiles and exports RowPair, RenderedLine, LineKind, RenderMode"
    - "At least 13 fixture .diff files exist under internal/diff/testdata/ covering every edge-case scenario"
    - "All fixture files use LF line endings (no CRLF)"
  artifacts:
    - go.mod
    - go.sum
    - .gitattributes
    - internal/diff/model.go
    - internal/diff/testdata/*.diff
  key_links:
    - "model.go type definitions are imported by every other file in package diff (parse/align/highlight/render)"
    - ".gitattributes eol=lf rule guarantees fixtures parse identically on Linux/macOS/Windows CI"
---

<objective>
Establish the Go module foundation and the test oracle for Phase 1: initialize the module with the three locked diff libraries, create the fixture corpus that all downstream tests assert against, and define the core type model (RowPair side-by-side structure per DIFF-01, RenderMode full-file/hunk-only enum per DIFF-05) that every other file in package `diff` imports.

Purpose: Nothing in the diff library can compile or be tested until the module exists, the type vocabulary is defined, and the fixture files (per D-01, D-02) are present. This plan is Wave 1 — the unblocking foundation.
Output: A buildable Go module, a 13-file fixture corpus under `internal/diff/testdata/`, and `internal/diff/model.go` with all shared types.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-diff-model/01-CONTEXT.md
@.planning/phases/01-diff-model/01-RESEARCH.md
@.planning/phases/01-diff-model/01-PATTERNS.md
@.claude/CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

Package: `github.com/alturd/alturd/internal/diff`

Files:
- `go.mod` — `module github.com/alturd/alturd`, `go 1.22`
- `go.sum` — checksums for the three locked dependencies
- `.gitattributes` — `internal/diff/testdata/*.diff text eol=lf`
- `internal/diff/model.go`
- `internal/diff/testdata/{simple,binary,rename,mode-only,submodule,no-newline,new-file,deleted-file,multi-file,multi-hunk,large-line,many-tokens,multiline-string}.diff`

Exported symbols (model.go):
- `type LineKind int` with constants `LineContext`, `LineAdded`, `LineRemoved`, `LineModifiedOld`, `LineModifiedNew`, `LineBlank`
- `type RenderMode int` with constants `FullFile` (iota 0 = default), `HunkOnly`
- `type RenderedLine struct { Kind LineKind; Content string; ANSI string }`
- `type RowPair struct { Left RenderedLine; Right RenderedLine }`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module with locked diff libraries</name>
  <files>go.mod, go.sum, .gitattributes</files>
  <read_first>
    - .claude/CLAUDE.md (Technology Stack table — exact versions for go-gitdiff, go-diff, chroma/v2)
    - .planning/phases/01-diff-model/01-RESEARCH.md (§Standard Stack, "Phase 1 Installation" block; §Environment Availability — Go version check)
  </read_first>
  <action>
    First verify the Go toolchain is available and new enough: run `go version` and confirm it reports 1.22 or higher. If `go` is absent or older than 1.22, STOP and report the missing toolchain — do not attempt workarounds (RESEARCH §Environment Availability documents `go install golang.org/dl/go1.22@latest` as the fallback).

    Initialize the module at repo root with module path `github.com/alturd/alturd` (placeholder per RESEARCH assumption A4) and Go directive `1.22`. Add ONLY the three Phase 1 libraries at their CLAUDE.md-locked versions: `github.com/bluekeyes/go-gitdiff@v0.8.1`, `github.com/sergi/go-diff@v1.4.0`, `github.com/alecthomas/chroma/v2@v2.27.0`. Do NOT add any bubbletea, lipgloss, bubbles, termenv, go-toml, or xdg dependency — those belong to Phases 3 and 4 (RESEARCH §"Not Used in Phase 1"). Run `go mod tidy` so `go.sum` is generated with checksums.

    Create `.gitattributes` at repo root with a rule that forces LF line endings on the fixture corpus: pattern `internal/diff/testdata/*.diff` with attributes `text eol=lf` (RESEARCH Pitfall 7 — prevents CRLF corruption on Windows checkout that would break parse tests).
  </action>
  <verify>
    <automated>go version && go build ./... && test -f go.sum && grep -q 'go-gitdiff v0.8.1' go.mod && grep -q 'chroma/v2 v2.27.0' go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `go version` reports 1.22 or higher (else task halts with a clear toolchain error)
    - `go.mod` declares `module github.com/alturd/alturd` and `go 1.22`
    - `go.mod` require block lists exactly: go-gitdiff v0.8.1, go-diff v1.4.0, chroma/v2 v2.27.0 — and NO bubbletea/lipgloss/bubbles/termenv/go-toml/xdg entries
    - `go.sum` exists with checksum lines for all three modules
    - `.gitattributes` contains a line matching `internal/diff/testdata/*.diff text eol=lf`
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Module builds, three locked deps resolved at exact versions, no Phase 3/4 deps present, .gitattributes enforces LF on fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Create the 13-scenario fixture corpus (D-01, D-02)</name>
  <files>internal/diff/testdata/simple.diff, internal/diff/testdata/binary.diff, internal/diff/testdata/rename.diff, internal/diff/testdata/mode-only.diff, internal/diff/testdata/submodule.diff, internal/diff/testdata/no-newline.diff, internal/diff/testdata/new-file.diff, internal/diff/testdata/deleted-file.diff, internal/diff/testdata/multi-file.diff, internal/diff/testdata/multi-hunk.diff, internal/diff/testdata/large-line.diff, internal/diff/testdata/many-tokens.diff, internal/diff/testdata/multiline-string.diff</files>
  <read_first>
    - .planning/phases/01-diff-model/01-RESEARCH.md (§Recommended Project Structure — fixture list; Pitfalls 4/5/6 for edge-case shapes; Open Question 1 — hand-craft if Python corpus unavailable; Open Question 2 — full-file context fixtures)
    - .planning/phases/01-diff-model/01-CONTEXT.md (D-01, D-02 — fixtures copied into testdata/, no runtime dependency on Python repo)
  </read_first>
  <action>
    Create the fixture corpus under `internal/diff/testdata/` per D-01 and D-02. If the Python implementation's `tests/fixtures/diff/` directory is reachable on this machine, copy the raw `.diff` files from there. If it is NOT reachable (Open Question 1), hand-craft each fixture as valid unified `git diff` output — every scenario type is fully documented in RESEARCH and produces standard git diff text.

    Create at minimum these 13 files, each a valid git diff exercising exactly one scenario:
    - `simple.diff` — a basic text change to a recognizable source file (use a `.md` named `README.md` so parse tests can assert NewName); include context + at least one delete and one add forming a Modified pair
    - `binary.diff` — a binary patch (`Binary files a/x and b/x differ` or a GIT binary patch block) so `IsBinary` is true
    - `rename.diff` — a pure rename with no content change (`rename from`/`rename to`, similarity 100%) so `IsRename` is true
    - `mode-only.diff` — a mode change with no text fragments (`old mode 100644` / `new mode 100755`) so `OldMode != NewMode` and `TextFragments` is empty (RESEARCH Pitfall 4)
    - `submodule.diff` — a submodule bump with mode `160000` and `-Subproject commit <sha>` / `+Subproject commit <sha>` lines (RESEARCH Pitfall 5)
    - `no-newline.diff` — a change whose final line is followed by `\ No newline at end of file` so `Line.NoEOL()` is true
    - `new-file.diff` — an added file (`new file mode`, all OpAdd lines) so `IsNew` is true
    - `deleted-file.diff` — a deleted file (`deleted file mode`, all OpDelete lines) so `IsDelete` is true
    - `multi-file.diff` — two files in one diff so Parse returns 2 File structs
    - `multi-hunk.diff` — a single file with two or more `@@` hunks separated by unchanged regions (supports HunkOnly vs FullFile distinction, DIFF-05)
    - `large-line.diff` — a modified line exceeding 1000 characters on at least one side (exercises the char guard, D-07)
    - `many-tokens.diff` — a modified line exceeding 200 whitespace/punctuation tokens but under 1000 chars (exercises the token guard, D-07)
    - `multiline-string.diff` — a change inside a multi-line string literal in a highlightable language (e.g. Go or Python) so highlight tests can detect chroma multi-line-token color bleed (RESEARCH Pitfall 2 / Open Question 3)

    For fixtures intended to test full-file rendering, generate with a large unified-context window so context lines are present (RESEARCH Open Question 2 / assumption A5). Save all files with LF line endings only.
  </action>
  <verify>
    <automated>test $(ls internal/diff/testdata/*.diff | wc -l) -ge 13 && ! grep -lUr $'\r' internal/diff/testdata/ && for f in simple binary rename mode-only submodule no-newline new-file deleted-file multi-file multi-hunk large-line many-tokens multiline-string; do test -s internal/diff/testdata/$f.diff || { echo "missing $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - At least 13 non-empty `.diff` files exist under `internal/diff/testdata/`, including every name listed in the action
    - No fixture file contains a carriage return (`\r`) byte
    - `large-line.diff` contains at least one changed line longer than 1000 characters
    - `submodule.diff` contains the `160000` mode marker and `Subproject commit` lines
    - `mode-only.diff` contains `old mode`/`new mode` lines and no `@@` hunk header
    - Each file is syntactically valid unified diff text (will be confirmed by parse tests in Plan 02)
  </acceptance_criteria>
  <done>All 13 scenario fixtures present, LF-only, each isolating one parse/render edge case per D-01/D-02.</done>
</task>

<task type="auto">
  <name>Task 3: Define the core type model (DIFF-01, DIFF-05)</name>
  <files>internal/diff/model.go</files>
  <read_first>
    - .planning/phases/01-diff-model/01-PATTERNS.md (§model.go — exact type and constant definitions to establish)
    - .planning/phases/01-diff-model/01-CONTEXT.md (D-03 renderer output contract, D-04 width parameter — informs that ANSI is stored per RenderedLine)
  </read_first>
  <action>
    Create `internal/diff/model.go` in `package diff` defining the shared type vocabulary that parse.go, align.go, highlight.go, and render.go all import.

    Define `type LineKind int` with iota constants in this exact order: `LineContext`, `LineAdded`, `LineRemoved`, `LineModifiedOld`, `LineModifiedNew`, `LineBlank` (the alignment filler). These encode the per-side classification produced by alignment.

    Define `type RenderMode int` with iota constants `FullFile` (value 0 — the default mode per DIFF-05, "entire file rendered with all unchanged lines shown") and `HunkOnly` (renders only changed hunks). FullFile must be iota 0 so the zero value is the documented default.

    Define `type RenderedLine struct` with fields `Kind LineKind`, `Content string` (raw text before highlighting), and `ANSI string` (the highlighted + diff-colored string, populated later by highlight.go/render.go). Define `type RowPair struct` with fields `Left RenderedLine` and `Right RenderedLine` — this is the side-by-side column model backing DIFF-01.

    Document each exported type with a short doc comment describing its role in the parse→align→highlight→render pipeline. Do not add any logic, functions, or imports beyond what type definitions require (this file is pure model — no go-gitdiff/chroma imports here).
  </action>
  <verify>
    <automated>go build ./internal/diff/ && go vet ./internal/diff/ && grep -q 'package diff' internal/diff/model.go</automated>
  </verify>
  <acceptance_criteria>
    - `internal/diff/model.go` is in `package diff` and compiles via `go build ./internal/diff/`
    - `LineKind` constants are declared in order: LineContext, LineAdded, LineRemoved, LineModifiedOld, LineModifiedNew, LineBlank
    - `RenderMode` declares FullFile as iota 0 (default) and HunkOnly as 1
    - `RenderedLine` has exactly fields Kind, Content, ANSI; `RowPair` has exactly fields Left, Right
    - `go vet ./internal/diff/` reports no issues
    - File contains no imports of go-gitdiff, go-diff, or chroma
  </acceptance_criteria>
  <done>Type model compiles and exports RowPair/RenderedLine/LineKind/RenderMode for downstream plans to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| go module proxy → local build | Third-party module source and checksums cross into the build |
| fixture file → test process | Fixture `.diff` bytes are read by `go test` (test-controlled, not end-user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `go get` of go-gitdiff / go-diff / chroma | high | mitigate | Pin exact versions (v0.8.1, v1.4.0, v2.27.0) per CLAUDE.md; `go mod tidy` records cryptographic checksums in go.sum; all three packages cleared OK in RESEARCH Package Legitimacy Audit |
| T-01-01 | Tampering | fixture file paths in tests | low | mitigate | Fixtures referenced by hardcoded relative `testdata/` paths only; never from user input (RESEARCH §Security Domain) |
| T-01-02 | Information Disclosure | placeholder module path | low | accept | `github.com/alturd/alturd` is a non-secret placeholder (A4); update on publish |
</threat_model>

<verification>
- `go version` >= 1.22
- `go build ./...` and `go vet ./internal/diff/` exit 0
- `go.sum` present with the three locked dependency checksums; no Phase 3/4 deps in go.mod
- 13+ LF-only fixtures present under internal/diff/testdata/
- model.go exports the four core types
</verification>

<success_criteria>
The module builds, the fixture corpus is complete and LF-normalized, and the type model compiles — unblocking Plan 02 (parse + align) and Plan 03 (highlight + render).
</success_criteria>

<output>
Create `.planning/phases/01-diff-model/01-01-SUMMARY.md` when done.
</output>
