# Phase 04.1: Address tech debt: Phase 3 human UAT + REQUIREMENTS.md doc-sync - Context

**Gathered:** 2026-08-13
**Status:** Ready for planning

<domain>
## Phase Boundary

Close out all 6 tech-debt items surfaced by `.planning/v1.0-MILESTONE-AUDIT.md` before the v1.0 milestone is archived. None are functional blockers (all 30 v1 requirements are independently satisfied per the audit), but the user chose to sweep the full list in this one phase rather than defer any of it:

1. **Phase 3 human visual TTY smoke check** — the blocking human checkpoint from `03-05-PLAN.md` Task 3 was never actually run; no `03-UAT.md` exists (unlike Phase 2 and Phase 4, which both recorded one).
2. **TREE-01 colored status marker check** — part of the same human session; confirms `[A]/[M]/[D]/[R]` render in distinct colors, not just correct text.
3. **REQUIREMENTS.md traceability doc-sync** — 8 rows (THEME-01, CONFIG-01, CONFIG-02, DIFFTOOL-02, DIFFTOOL-03, DIST-01, DIST-02, DIST-03) show stale `Status` column text ("Gaps Found") that contradicts their own `[x]` checkboxes and `04-VERIFICATION.md`'s independent SATISFIED verdict.
4. **WR-07** — `loadDifftoolFiles` (`cmd/alturd/main.go`) always reads `--difftool-remote` into `DifftoolInfo.NewFileLines`, but `AlignFull`'s documented contract (`internal/diff/align.go:255-261`) requires old-file content for deletions. Currently masked by a coincidence (whole-file deletions always parse as one hunk); no regression test guards it.
5. **IN-01** — `gofmt -l` flags `cmd/alturd/main.go`, `internal/diff/render.go`, `internal/tui/model.go`. `.golangci.yml` has no `formatters` block (golangci-lint v2 moved gofmt/goimports there), so CI doesn't catch this.
6. **Nyquist Phase 4 reconciliation** — `04-VALIDATION.md` still shows `status: draft` / `nyquist_compliant: false` because `/gsd-validate-phase 4` was never re-run after the `04-07` gap-closure plan landed. Bookkeeping only — `04-07` shipped its own genuine regression test, independently re-verified by `04-VERIFICATION.md`.

Out of scope: any new capability, and IN-02/IN-03 (other `04-REVIEW.md` Info-severity findings not named in the milestone audit's tech-debt table).

</domain>

<decisions>
## Implementation Decisions

### Scope
- **D-01:** All 6 tech-debt items ride together in this phase — the user explicitly chose the full sweep over the narrower "2 titled items only" option.

### Phase 3 Human UAT
- **D-02:** Executed as a **live interactive session** with the user — Claude hands over the exact checklist from `03-05-PLAN.md` Task 3 (`checkpoint:human-verify`), the user runs the real binary in their own terminal, and reports pass/fail back. **Not** a scripted pty automation (unlike `04-UAT.md`'s difftool test) — pty automation cannot confirm color rendering, which is exactly the unverified item.
- **D-03:** Produces a `04.1-UAT.md` recording the pass, matching the existing `02-UAT.md` / `04-UAT.md` convention. — **Reversibility:** reversible — just a doc.
- The checklist to run verbatim is in `03-05-PLAN.md` (`<task type="checkpoint:human-verify">`, steps 1-10: split screen, Tab resize, `]`/`[` cycle, `n`/`N` hunk nav, `v` toggle, `/` search, `a` full-repo toggle, `q` exit, `--version`/`--help`), plus the TREE-01 color check from `03-VERIFICATION.md` (`[A]/[M]/[D]/[R]` markers in distinct colors, not plain text).

### REQUIREMENTS.md Doc-Sync
- **D-04:** Scoped to exactly the 8 known-stale `Status` column entries (THEME-01, CONFIG-01, CONFIG-02, DIFFTOOL-02, DIFFTOOL-03, DIST-01, DIST-02, DIST-03) — change "Gaps Found" → "Complete". **Not** a full re-audit of the traceability table; the milestone audit already cross-checked the rest clean.

### WR-07 Fix
- **D-05:** Straightforward bug fix, no separate investigation phase — user confirmed intent, treat as correctness bug. Fix per `04-REVIEW.md`'s prescribed patch: in `loadDifftoolFiles` (`cmd/alturd/main.go:261-264`), branch on `files[0].IsDelete` and read from `local` instead of `remote` when true. Add a regression test exercising FullFile mode against a deleted file in difftool mode (none currently exists). — **Reversibility:** reversible — isolated to one function + new test.
- Consider (per `04-REVIEW.md`) renaming `DifftoolInfo.NewFileLines` since it no longer always holds "new" content — left to planner/executor discretion, not a locked decision.

### IN-01 Fix
- **D-06:** Fix files now (`gofmt -w cmd/alturd/main.go internal/diff/render.go internal/tui/model.go` — note: 3 files per `04-REVIEW.md`, not 2 as the milestone audit summary said) **and** harden CI: add a `formatters` block enabling `gofmt`/`goimports` to `.golangci.yml` so future drift fails CI instead of passing silently. — **Reversibility:** reversible.

### Nyquist Reconciliation
- **D-07:** Included as a task inside this phase's plan (not run separately by the user) — invoke `/gsd-validate-phase 4` (or equivalent Nyquist validation step) to flip `04-VALIDATION.md` from `status: draft` to compliant.

### Claude's Discretion
- Ordering of the 6 items within the plan (e.g., whether REQUIREMENTS.md doc-sync happens before or after the UAT session — the audit already independently confirms SATISFIED status regardless of UAT outcome, so no hard dependency).
- Whether to rename `DifftoolInfo.NewFileLines` alongside the WR-07 fix.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Tech debt source of truth
- `.planning/v1.0-MILESTONE-AUDIT.md` — all 6 items, severities, and rationale (frontmatter `tech_debt:` block + §5 Tech Debt Summary table)

### Phase 3 Human UAT
- `.planning/phases/03-tui-application/03-05-PLAN.md` — `<task type="checkpoint:human-verify">` (Task 3) contains the exact 10-step checklist to run verbatim
- `.planning/phases/03-tui-application/03-VERIFICATION.md` — §"Human Verification Required" items 1-3 (visual TTY smoke check, TREE-01 colored markers, TREE-02 instant-vs-animated — item 3 already resolved via PROJECT.md decision, not part of this phase's UAT)
- `.planning/phases/02-git-layer-cli/02-UAT.md` and `.planning/phases/04-config-theming-difftool-distribution/04-UAT.md` — format convention for the new `04.1-UAT.md`

### REQUIREMENTS.md Doc-Sync
- `.planning/REQUIREMENTS.md` — traceability table, the 8 stale rows to fix
- `.planning/phases/04-config-theming-difftool-distribution/04-VERIFICATION.md` — independent SATISFIED verdict for all 9 Phase 4 requirements, source of truth for the correct status text

### WR-07 and IN-01
- `.planning/phases/04-config-theming-difftool-distribution/04-REVIEW.md` — §WR-07 (line 138) full repro, root cause, and prescribed patch; §IN-01 (line 167) exact 3 files and prescribed `.golangci.yml` fix
- `internal/diff/align.go:255-261` — `AlignFull`'s doc comment defining the old-file-for-deletions contract that WR-07 violates
- `cmd/alturd/main.go:261-264` — `loadDifftoolFiles`, the WR-07 fix site
- `internal/tui/model.go:934-940` — `fetchFileLines`, the standalone (non-difftool) code path that already gets this right — reference implementation for the fix
- `.golangci.yml` — needs a `formatters` block added for IN-01

### Nyquist Reconciliation
- `.planning/phases/04-config-theming-difftool-distribution/04-VALIDATION.md` — current `status: draft` to be reconciled

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `03-05-PLAN.md`'s human-verify checklist is already written and detailed — no need to redesign the UAT steps, just execute them.
- `04-UAT.md` / `02-UAT.md` provide a ready template/format for the new `04.1-UAT.md`.

### Established Patterns
- `internal/tui/model.go:934-940` (`fetchFileLines`) is the existing correct pattern for old-vs-new file source selection on deletion — the WR-07 fix should mirror this same `IsDelete` branch in the difftool path.
- `.golangci.yml` currently has `staticcheck`, `govet`, `errcheck`, `revive` enabled but no `formatters` block (golangci-lint v2 structure).

### Integration Points
- WR-07 fix touches `cmd/alturd/main.go` (`loadDifftoolFiles`) only — `internal/diff/align.go`'s `AlignFull` contract is unchanged, just finally honored correctly.
- IN-01 fix touches 3 files' formatting + `.golangci.yml` config only, no logic changes.
- Nyquist reconciliation touches only `04-VALIDATION.md` frontmatter via the validate-phase workflow — no source code changes.

</code_context>

<specifics>
## Specific Ideas

No additional specifics beyond the locked decisions above — this phase is a well-defined cleanup sweep with each item's fix already prescribed by prior audit/review artifacts.

</specifics>

<deferred>
## Deferred Ideas

- **IN-02** (`install-difftool` writes bare `alturd`, relying on `PATH`) — Info-severity, not in the milestone audit's tech-debt table, not part of this phase. Possibly an accepted trade-off per `04-REVIEW.md`'s own framing.
- **IN-03** (`os.ReadFile(remote)` error silently discarded in `loadDifftoolFiles`) — Info-severity, not in the milestone audit's tech-debt table, not part of this phase.
- **WR-02** (`diffW` going negative on narrow terminal with tree focused) — flagged as a known-but-unaddressed gap in `model.go:336`'s own comments; not in the milestone audit's tech-debt table, not part of this phase.

### Reviewed Todos (not folded)
None — no pending todos matched this phase.

</deferred>

---

*Phase: 04.1-address-tech-debt-phase-3-human-uat-requirements-md-doc-sync*
*Context gathered: 2026-08-13*
