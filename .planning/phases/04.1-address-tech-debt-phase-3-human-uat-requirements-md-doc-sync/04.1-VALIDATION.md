---
phase: 04.1
slug: address-tech-debt-phase-3-human-uat-requirements-md-doc-sync
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-17
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Go standard `testing` package, consistent with all prior phases; plus two hand-checked planning documents (`REQUIREMENTS.md`, `04.1-UAT.md`) verified by `grep`/`sed` shape assertions |
| **Config file** | none — `go test ./...` requires no config; `.golangci.yml` gates lint/format |
| **Quick run command** | `go test ./cmd/alturd/... -run TestLoadDifftoolFilesDeletedFileUsesLocalSide -v` |
| **Full suite command** | `go build ./... && go vet ./... && go test ./... && gofmt -l . && golangci-lint run ./...` |
| **Estimated runtime** | ~1s test suite; ~2-4s including `golangci-lint run ./...` |

> Note: `-race` requires CGO, unavailable in this execution sandbox (`CGO_ENABLED=0`, no `gcc`/`cc` on `PATH`, no root to install one). `go test ./...` (non-race) is fully green; the `-race` gap is tracked as an open broken-window item (see Manual-Only Verifications below), not silently dropped.

---

## Sampling Rate

- **After every task commit:** Run the package(s) the task touched, e.g. `go test ./cmd/alturd/...`
- **After every plan wave:** Run `go build ./... && go vet ./... && go test ./... && gofmt -l . && golangci-lint run ./...`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.1-01-T1 | 01 | 1 | DIFF-05, DIFFTOOL-01 | T-04.1-02 / T-04.1-04 | Deleted file in difftool mode sources reference content from `--difftool-local`, not `--difftool-remote` (WR-07 fix); sensitivity control fails if the branch is inverted | unit (whitebox, `package main`) | `go test ./cmd/alturd/... -run TestLoadDifftoolFilesDeletedFileUsesLocalSide -v` | ✅ | ✅ green |
| 04.1-01-T2 | 01 | 1 | DIST-01 | T-04.1-01 | Module is gofmt-clean; `.golangci.yml` `formatters:` block enforces it (`gofmt`, `goimports`), additive to the untouched `linters:` block | other (fmt/lint gate) | `gofmt -l . \|\| true; golangci-lint fmt --diff ./...` | ✅ | ✅ green |
| 04.1-01-T3 | 01 | 1 | DIST-01 | — | `golangci-lint run ./...` exits 0 with zero issues (baseline before this task: 7 — 5 errcheck, 2 revive); no `nolint` suppression used | other (lint gate) | `golangci-lint run ./...` | ✅ | ✅ green |
| 04.1-02-T1 | 02 | 2 | THEME-01, CONFIG-01, CONFIG-02, DIFFTOOL-02, DIFFTOOL-03, DIST-01, DIST-02, DIST-03 | T-04.1-06 | `REQUIREMENTS.md` traceability Status cells synced `Gaps Found` → `Complete` for the 8 stale rows, agreeing with `04-VERIFICATION.md`'s independent SATISFIED verdicts; targeted 9-line diff | other (doc-shape check) | `sed -n '/^## Traceability/,/^\*\*Coverage/p' .planning/REQUIREMENTS.md \| grep -c '\| Complete \|'` → 30 | ✅ | ✅ green |
| 04.1-02-T2 | 02 | 2 | — (bookkeeping; reconciles DIFFTOOL-01/DIFF-05 coverage record) | T-04.1-07 / T-04.1-08 | Phase 4's `04-VALIDATION.md` reconciled by a real `validate-phase` run against the post-`04.1-01` tree: `status: draft` → `validated`, `nyquist_compliant: true` as a genuine finding, not forced | other (doc-shape check + zero code diff) | `grep -qE '^status: (validated\|complete)$' 04-VALIDATION.md && test -z "$(git diff --name-only -- '*.go' .golangci.yml .github)"` | ✅ | ✅ green |
| 04.1-03-T1 | 03 | 2 | DIFF-06, NAV-01, NAV-02, NAV-03, NAV-04, TREE-01, TREE-02, TREE-03, SEARCH-01 | T-04.1-12 | Live human TTY session confirms split-screen TUI launch, resize, file cycle, hunk nav, mode toggle, search, full-repo toggle, clean exit, and TREE-01 marker colour — the one assertion structurally unautomatable by `go test` or pty capture | manual (`checkpoint:human-verify`, live session) | N/A — real interactive terminal required; recorded in `04.1-UAT.md` | ✅ (`04.1-UAT.md`) | ✅ green (11/11 items pass) |
| 04.1-03-T2 | 03 | 2 | — (bookkeeping; records the T1 session outcome) | T-04.1-13 | `04.1-UAT.md` records all 11 checklist items with `result:`/`verified_by:` attributing observation to the human, matching `02-UAT.md`/`04-UAT.md` convention; no source file touched | other (doc-shape check) | `grep -c '^### [0-9]' 04.1-UAT.md` → 11; `grep -c '^result:' 04.1-UAT.md` → 11; `grep -c 'pending: 0' 04.1-UAT.md` → 1 | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

All 7 rows above were independently re-run against the tree as it stands at this reconciliation pass: `go build ./...`, `go vet ./...`, and `go test ./...` are green across all 6 packages; `gofmt -l .` produces no output; `golangci-lint run ./...` (v2.7.2, matching the CI-pinned version) reports 0 issues; `REQUIREMENTS.md`'s traceability table has all 30 rows reading `Complete`; `04-VALIDATION.md` carries `status: validated` / `nyquist_compliant: true`; `04.1-UAT.md` has 11/11 `result: pass` entries with `pending: 0` and no `## Gaps` section.

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test framework or fixture bootstrapping was needed:

- [x] `cmd/alturd/loaddifftoolfiles_internal_test.go` — new WR-07 regression test file, created by 04.1-01-T1 itself (present, both subtests green)
- [x] `go test` / `gofmt` / `golangci-lint` — all pre-wired since Phase 4 (04-VALIDATION.md), no fresh install needed
- [x] `04.1-UAT.md` — new UAT report file, created by 04.1-03-T2, following the `02-UAT.md`/`04-UAT.md` shape convention (present, 11/11 pass)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions | Status |
|----------|-------------|------------|--------------------|--------|
| Live TUI rendering: split-screen layout, resize, file/hunk nav, search, full-repo toggle, clean exit, and `[A]/[M]/[D]/[R]` marker colour | DIFF-06, NAV-01, NAV-02, NAV-03, NAV-04, TREE-01, TREE-02, TREE-03, SEARCH-01 | Requires a real alternate-screen TTY; colour rendering in particular cannot be confirmed by `go test` assertions or scripted pty capture (D-02) | Run `alturd` in a git repo with 3+ changed files; work through the 11-step checklist in `04.1-UAT.md` | Resolved — `04.1-UAT.md`: all 11 items reported `pass` by a live human session, 2026-08-14 |
| `go test -race ./...` (full suite, race detector) | DIST-01 (CI correctness); exercises all packages incl. `cmd/alturd`, `internal/tui`, `internal/diff` | `-race` requires CGO; this execution sandbox has `CGO_ENABLED=0`, no `gcc`/`cc` on `PATH`, and no root to install one | Re-run `go test -race ./...` in an environment with a working C toolchain (e.g. CI's Linux/macOS runners) | **Open** — tracked in `.planning/WINDOWS.md` id 2 (`unrun-verify`, phase `04.1`, recorded 2026-08-14). `go test ./...` (non-race) is fully green and was run at every verification point in its place. Not resolved by this validation pass — a sandbox constraint, not a missing test. |
| Real 3-OS CI matrix / live tag-triggered GitHub Release | DIST-01, DIST-02, DIST-03 | GitHub Actions runners are the only true multi-OS environment; not reproducible from this sandbox | Push branch/tag, confirm all 3 OS jobs and the Release workflow pass | Out of this phase's scope — already recorded resolved in `04-VALIDATION.md` (Phase 4), unaffected by 04.1's doc-only changes to those requirements' traceability status |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-08-17

---

## Validation Audit 2026-08-17

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

State B reconstruction (no pre-existing `04.1-VALIDATION.md`; built from `04.1-01-SUMMARY.md`, `04.1-02-SUMMARY.md`, `04.1-03-SUMMARY.md`, and their source `PLAN.md` files). Independently re-verified rather than trusting the SUMMARY claims alone: `go build ./...`, `go vet ./...`, `go test ./...` (all 6 packages `ok`), `gofmt -l .` (empty), `golangci-lint run ./...` v2.7.2 (0 issues, matching the CI-pinned version), `REQUIREMENTS.md` traceability (30/30 `Complete`), `04-VALIDATION.md` (`status: validated`, `nyquist_compliant: true`), and `04.1-UAT.md` (11/11 `pass`, `pending: 0`, no `## Gaps`) — all confirmed on disk, no gap found. `go test -race ./...` remains blocked by the sandbox's missing C toolchain and is carried forward as `.planning/WINDOWS.md` id 2 (open, unrun-verify) rather than marked resolved; this is an infrastructure constraint, not a missing test, and is recorded above under Manual-Only Verifications rather than hidden.
