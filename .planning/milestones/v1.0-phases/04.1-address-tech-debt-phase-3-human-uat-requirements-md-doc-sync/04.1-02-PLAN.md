---
phase: 04.1-address-tech-debt-phase-3-human-uat-requirements-md-doc-sync
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/04-config-theming-difftool-distribution/04-VALIDATION.md
autonomous: true
requirements: [THEME-01, CONFIG-01, CONFIG-02, DIFFTOOL-02, DIFFTOOL-03, DIST-01, DIST-02, DIST-03]

must_haves:
  truths:
    - "A reader of .planning/REQUIREMENTS.md alone can no longer misread 8 Phase 4 requirements as failing — every traceability Status cell agrees with its own [x] checkbox and with 04-VERIFICATION.md's independent SATISFIED verdict (D-04)"
    - "Exactly the 8 known-stale rows change; the other 22 traceability rows, all 30 checkboxes, every requirement description, and the Coverage block are byte-identical afterwards (D-04 — this is a targeted sync, not a re-audit)"
    - "04-VALIDATION.md is no longer status: draft — the Nyquist bookkeeping debt from v1.0-MILESTONE-AUDIT.md §4 is closed by an actual validate-phase run rather than a hand-edit (D-07)"
    - "The Phase 4 validation record accounts for coverage that exists today, including the 04-07 gap-closure regression test and the WR-07 regression test landed by 04.1-01"
  artifacts:
    - ".planning/REQUIREMENTS.md — traceability table with all 30 rows reading Complete"
    - ".planning/phases/04-config-theming-difftool-distribution/04-VALIDATION.md — reconciled frontmatter and a filled Per-Task Verification Map"
  key_links:
    - ".planning/REQUIREMENTS.md traceability Status column -> .planning/phases/04-config-theming-difftool-distribution/04-VERIFICATION.md SATISFIED verdicts — the source of truth the stale cells contradict"
    - "cmd/alturd/loaddifftoolfiles_internal_test.go (created by 04.1-01) -> 04-VALIDATION.md Per-Task Verification Map — new DIFFTOOL-01/DIFF-05 coverage that must exist on disk before validate-phase samples it"
---

<objective>
Close the two pure-bookkeeping tech-debt items from `.planning/v1.0-MILESTONE-AUDIT.md`: the **stale REQUIREMENTS.md traceability table** (8 Phase 4 rows whose Status text contradicts their own checkbox and the independent verification verdict) and the **unreconciled Nyquist record for Phase 4** (`04-VALIDATION.md` still carries the `status: draft` seeded at plan time, because `/gsd-validate-phase 4` was never re-run after the `04-07` gap-closure plan landed).

Purpose: neither item is a functional gap — the milestone audit confirms all 30 v1 requirements independently satisfied — but both are traps for the next reader. Someone opening `REQUIREMENTS.md` before archival sees 8 requirements that look like failures, and someone auditing Nyquist coverage sees Phase 4 flagged NOT-VALIDATED. Both records should say what is actually true before v1.0 is archived.

Output: a traceability table that agrees with itself, and a Phase 4 validation record produced by a real validation run.

**Why this plan waits on 04.1-01.** The REQUIREMENTS.md edit has no dependency on anything and could run in Wave 1. The Nyquist task does: `/gsd-validate-phase 4` samples the coverage that exists on disk when it runs, and `04.1-01` adds a new regression test for DIFFTOOL-01/DIFF-05 (`TestLoadDifftoolFilesDeletedFileUsesLocalSide`). Running validation first would produce a record that is stale the moment it is written. Plan-level `depends_on` reflects the stricter of the two.

**Requirement IDs.** Phase 04.1 has no requirement IDs of its own (ROADMAP shows `Requirements: TBD`). The 8 IDs listed above are exactly the 8 pre-existing v1 requirements whose *records* this plan corrects — they are already `[x]` satisfied and this plan does not newly satisfy them.

**Spec-less probe fallback: skipped (visible, recorded skip)** — no SPEC.md and no requirement IDs for the edge-probe engine to derive predicates from; `must_haves.truths` come from `04.1-CONTEXT.md`'s decisions instead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-address-tech-debt-phase-3-human-uat-requirements-md-doc-sync/04.1-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync the 8 stale Phase 4 rows in the REQUIREMENTS.md traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the whole file, and specifically the `## Traceability` table (starts around line 91). Its 30 data rows use the shape `| <ID> | Phase <N> | <Status> |`. The 8 rows to change are at lines 114-116 and 118-122; line 117 (DIFFTOOL-01) is already correct and must not be touched.
    - `.planning/phases/04-config-theming-difftool-distribution/04-VERIFICATION.md` — the Requirements Coverage table, the independent SATISFIED verdict for all 9 Phase 4 requirements that the stale cells contradict
    - `.planning/v1.0-MILESTONE-AUDIT.md` §2 — the 3-source cross-reference confirming all 30 requirements satisfied, and its explicit framing of these 8 cells as a documentation-sync item rather than an unsatisfied requirement
  </read_first>
  <action>
    <!-- planner-discipline-allow: Gaps Found -->
    In `.planning/REQUIREMENTS.md`'s `## Traceability` table, change the Status cell to `Complete` for exactly these 8 requirement IDs, per D-04: THEME-01, CONFIG-01, CONFIG-02, DIFFTOOL-02, DIFFTOOL-03, DIST-01, DIST-02, DIST-03. Each currently reads `Gaps Found`. `Complete` is the exact string the other 22 rows already use — copy that wording rather than inventing a new one; the substantive justification for it is `04-VERIFICATION.md`'s SATISFIED verdict for all 9 Phase 4 requirements.

    Locate the rows by grepping each requirement ID rather than by line number before editing, and apply targeted per-row replacements.

    Bounded scope, per D-04 — this is a targeted sync, not a re-audit. Do **not**: change DIFFTOOL-01's row (already `Complete`); change any row for Phases 1-3; change any of the 30 `- [x]` checkboxes in the `## v1 Requirements` sections; reword any requirement description; touch the `## v2 Requirements`, `## Out of Scope`, or `**Coverage:**` blocks; add or remove table rows; or re-verify any requirement. The milestone audit already cross-checked the remaining 22 rows clean.

    Update the trailing `*Last updated: ...*` line at the bottom of the file to today's date with a short note that the Phase 4 traceability Status cells were synced to `04-VERIFICATION.md`, preserving the existing `*Requirements defined: 2026-06-26*` line above it.
  </action>
  <verify>
    <automated>test "$(sed -n '/^## Traceability/,/^\*\*Coverage/p' .planning/REQUIREMENTS.md | grep -c '| Complete |')" = "30" &amp;&amp; test "$(grep -c '^- \[x\] \*\*' .planning/REQUIREMENTS.md)" = "30"</automated>
  </verify>
  <acceptance_criteria>
    - Within the `## Traceability` table region, `grep -c '| Complete |'` returns 30 — every one of the 30 data rows now reads `Complete`
    - No Status cell in that region reads anything other than `Complete`: `sed -n '/^## Traceability/,/^\*\*Coverage/p' .planning/REQUIREMENTS.md | grep -E '^\| [A-Z]' | grep -vc '| Complete |'` returns 1 (the header row only)
    - All 30 requirement checkboxes are still checked and none was added or removed: `grep -c '^- \[x\] \*\*' .planning/REQUIREMENTS.md` returns 30 and `grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md` returns 0
    - The `**Coverage:**` block is unchanged — it still states 30 total, 30 mapped, 0 unmapped
    - `git diff --stat .planning/REQUIREMENTS.md` shows 9 changed lines or fewer (8 table rows plus the last-updated line) — proof the edit stayed targeted
    - `git diff .planning/REQUIREMENTS.md` contains no change to any line beginning `- [x]` or `- [ ]`, and no change to any `| DIFF-`, `| GIT-`, `| NAV-`, `| TREE-`, `| SEARCH-`, `| LOG-`, or `| DIFFTOOL-01 |` row
  </acceptance_criteria>
  <reversibility rating="reversible">Text-cell edits in one planning document.</reversibility>
  <done>Every row of the traceability table agrees with its own checkbox and with `04-VERIFICATION.md`, and the diff proves nothing outside those 8 cells moved.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the Phase 4 Nyquist validation record by running validate-phase</name>
  <files>.planning/phases/04-config-theming-difftool-distribution/04-VALIDATION.md</files>
  <precondition>`04.1-01` is committed, so `cmd/alturd/loaddifftoolfiles_internal_test.go` exists on disk and `go test ./...` is green — validate-phase samples the test coverage present when it runs, and a run against a red or pre-fix tree would record a validation state that is wrong on arrival.</precondition>
  <read_first>
    - `.planning/phases/04-config-theming-difftool-distribution/04-VALIDATION.md` — the whole file: frontmatter (`status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`), the Per-Task Verification Map whose Task ID / Plan / Wave columns are all still `TBD` and whose Status column is all `⬜ pending`, the Wave 0 Requirements checklist, and the unticked Validation Sign-Off block. The frontmatter comment states the intended lifecycle: `draft (seeded by plan-phase) → validated (set by validate-phase §6)`.
    - `.planning/v1.0-MILESTONE-AUDIT.md` §4 and the `nyquist:` frontmatter block — the classification of Phase 4 as a NOT-VALIDATED coverage TODO (not a demonstrated compliance failure), and the reason: validate-phase was never re-run after `04-07`
    - `.planning/phases/04-config-theming-difftool-distribution/04-VERIFICATION.md` — the independent re-verification after gap closure, including the fault-injected `TestGitDifftoolExitsCleanlyWhenBackendAborts`
    - `.planning/phases/04.1-.../04.1-01-SUMMARY.md` — what `04.1-01` actually landed, so the new DIFFTOOL-01/DIFF-05 coverage can be accounted for
  </read_first>
  <action>
    Per D-07, reconcile Phase 4's Nyquist record by invoking the validation workflow itself rather than hand-editing frontmatter: run the `gsd-validate-phase` skill with argument `4`. Let that workflow own what it writes to `.planning/phases/04-config-theming-difftool-distribution/04-VALIDATION.md` — the Per-Task Verification Map's Task ID / Plan / Wave / Status columns, the Wave 0 Requirements checklist, the Validation Sign-Off block, and the `status` / `nyquist_compliant` / `wave_0_complete` frontmatter fields.

    Two things to feed it, since it is reconciling a phase that finished three plans ago:
    - Phase 4 shipped 7 plans (`04-01` through `04-07`); `04-07` was the G-04-1 gap closure and carries its own end-to-end regression test, `TestGitDifftoolExitsCleanlyWhenBackendAborts`, which `04-VERIFICATION.md` independently re-ran and fault-injected. That test post-dates the wave-based validation the draft record was seeded from and is the specific reason the record is stale.
    - `04.1-01` has just added `TestLoadDifftoolFilesDeletedFileUsesLocalSide` in `cmd/alturd/loaddifftoolfiles_internal_test.go`, which is new automated coverage for the DIFFTOOL-01 / DIFF-05 full-file difftool path.

    Scope boundary: reconcile Phase 4's record only. Do not touch `01-VALIDATION.md`, `02-VALIDATION.md`, or `03-VALIDATION.md` — the milestone audit already records all three as compliant. Do not modify any Go source, test, or CI file; this task is bookkeeping and must produce no code diff.

    **If validation comes back non-compliant, do not force the field to true and do not weaken the validation to make it pass.** Record the genuine finding in the plan SUMMARY and surface it — the milestone audit's position is that Phase 4 is a coverage TODO rather than a demonstrated failure, so a real compliance gap here would be new information that the developer needs, not a formality to paper over.
  </action>
  <verify>
    <automated>grep -qE '^status: (validated|complete)$' .planning/phases/04-config-theming-difftool-distribution/04-VALIDATION.md &amp;&amp; test -z "$(git diff --name-only -- '*.go' .golangci.yml .github)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^status: draft$' .planning/phases/04-config-theming-difftool-distribution/04-VALIDATION.md` returns 0 — the seeded draft state is gone, replaced by the workflow's own terminal status
    - The Per-Task Verification Map no longer carries placeholder rows: within that table, no data row has `TBD` in its Task ID, Plan, or Wave column, and no data row's Status cell is still `⬜ pending`
    - The Validation Sign-Off block's checkboxes are resolved rather than all blank, and `**Approval:**` no longer reads `pending`
    - `nyquist_compliant` is set to the value the validation run actually determined. If it is `false`, the plan SUMMARY names the specific uncovered behaviours — an unexplained `false`, or a `true` written without a validation run behind it, fails this task.
    - `git diff --name-only` lists no `.go` file, no `.golangci.yml`, and nothing under `.github/` — this task produced zero code change
    - `git diff --name-only` lists no `VALIDATION.md` for Phases 1, 2, or 3
    - `go test ./...` still passes (guard against the validation workflow leaving a stray or edited test behind)
  </acceptance_criteria>
  <reversibility rating="reversible">Planning-document frontmatter and table reconciliation; no code touched.</reversibility>
  <done>`04-VALIDATION.md` reflects a real validation run against the tree as it stands after `04.1-01`, the `status: draft` bookkeeping debt from the milestone audit is closed, and no source file moved.</done>
</task>

</tasks>

<threat_model>
Security enforcement is active. ASVS level: **1**. Blocking severity: **high**. This plan edits only files under `.planning/` and produces no executable change, so no threat reaches high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → human reader | `REQUIREMENTS.md` and `04-VALIDATION.md` are the artifacts a future reader, auditor, or milestone-archival step trusts to describe what is verified. A wrong record is a correctness problem for the humans downstream, not for the binary. |
| validate-phase workflow → repository | The `gsd-validate-phase` skill runs with write access to the repo; its intended blast radius is one VALIDATION.md. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-06 | Repudiation | REQUIREMENTS.md traceability sync (Task 1) | low | mitigate | The Status change is justified by an independent source (`04-VERIFICATION.md`'s SATISFIED verdicts) rather than by the executor's own judgement, D-04 caps the edit at 8 named rows, and the diff-size acceptance criterion (9 lines or fewer) makes an over-broad "everything is fine now" sweep detectable in review. |
| T-04.1-07 | Tampering | validate-phase blast radius (Task 2) | low | mitigate | Acceptance criteria assert `git diff --name-only` lists no `.go` file, nothing under `.github/`, and no Phase 1-3 VALIDATION.md — so a workflow that wandered outside Phase 4's record is caught before commit. `go test ./...` is re-run as a guard against edited or stray tests. |
| T-04.1-08 | Repudiation | Falsely asserting Nyquist compliance (Task 2) | low | mitigate | The task explicitly forbids forcing `nyquist_compliant: true` or weakening validation to pass, and requires a `false` result to be explained by name in the SUMMARY. The verifiable state is "no longer `status: draft`", not "compliant" — so the plan cannot be satisfied by fabricating a verdict. |
| T-04.1-SC | Tampering | Supply chain | low | mitigate | No package installs, no dependency changes, no code execution introduced by this plan. Verified by acceptance: `git diff --name-only` lists no `.go` file, no `go.mod`, and no `go.sum`. |
</threat_model>

<verification>
- Within `REQUIREMENTS.md`'s traceability table, all 30 rows read `Complete` and all 30 requirement checkboxes remain `[x]`
- `git diff --stat .planning/REQUIREMENTS.md` shows 9 changed lines or fewer
- `04-VALIDATION.md` no longer carries `status: draft`, its Per-Task Verification Map has no `TBD` or `⬜ pending` cells, and its sign-off block is resolved
- `git diff --name-only` lists no `.go` file, no `go.mod`/`go.sum`, nothing under `.github/`, and no Phase 1-3 VALIDATION.md
- `go test ./...` still passes
</verification>

<success_criteria>
- A reader of `REQUIREMENTS.md` alone can no longer mistake 8 satisfied Phase 4 requirements for failures (D-04)
- Phase 4's Nyquist record is the product of an actual validation run against the post-`04.1-01` tree, not a hand-edited field (D-07)
- Both edits are provably confined to `.planning/` — zero code diff
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Note |
|----------|------|------|
| `.planning/REQUIREMENTS.md` traceability table | 8 modified Status cells | THEME-01, CONFIG-01, CONFIG-02, DIFFTOOL-02, DIFFTOOL-03, DIST-01, DIST-02, DIST-03 → `Complete`; the `*Last updated: ...*` footer line is refreshed |
| `.planning/phases/04-config-theming-difftool-distribution/04-VALIDATION.md` | reconciled frontmatter | `status` (out of `draft`), `nyquist_compliant`, `wave_0_complete` set by the validate-phase run |
| `04-VALIDATION.md` Per-Task Verification Map | filled table columns | Task ID / Plan / Wave / Status columns replace the seeded `TBD` and `⬜ pending` placeholders |
| `04-VALIDATION.md` Validation Sign-Off | resolved checklist | `**Approval:**` no longer reads `pending` |

<output>
Create `.planning/phases/04.1-address-tech-debt-phase-3-human-uat-requirements-md-doc-sync/04.1-02-SUMMARY.md` when done
</output>
