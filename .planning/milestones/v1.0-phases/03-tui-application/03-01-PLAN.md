---
phase: 03-tui-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/diff/render.go
  - internal/diff/render_test.go
  - internal/diff/align.go
  - internal/diff/align_test.go
  - cmd/alturd/main.go
autonomous: true
requirements: [DIFF-06, NAV-01]
must_haves:
  truths:
    - "diff.Render accepts a RenderMode argument so the TUI 'v' key can request FullFile or HunkOnly without reparsing git data (DIFF-06)"
    - "diff.HunkStartRows returns ascending 0-based row indices marking where each hunk begins in the Align/Render output (NAV-01)"
    - "Edge-case files (binary, mode-only, submodule) report exactly one hunk starting at row 0"
    - "The whole module still compiles after the Render signature change"
  artifacts:
    - "internal/diff/render.go — Render(file, width, mode) 3-arg signature"
    - "internal/diff/align.go — HunkStartRows + countFragmentRows functions"
    - "internal/diff/align_test.go — TestHunkStartRows table tests"
  key_links:
    - "countFragmentRows row accounting must exactly mirror alignText so viewport SetYOffset centers the correct hunk"
    - "cmd/alturd/main.go Render call updated to 3-arg form so go build ./... stays green until Plan 05 rewires main"
---

<objective>
Extend the existing internal/diff library with the two capabilities the TUI needs before any bubbletea code exists: a mode-parameterized Render() (DIFF-06) and a HunkStartRows() function that reports hunk boundary row indices (NAV-01). This is a pure-Go, fully unit-testable change with no TUI dependencies.

Purpose: The TUI model (Plan 03/04) calls diff.Render(file, width, mode) on every 'v' toggle and diff.HunkStartRows(file, mode) to drive 'n'/'N' hunk navigation. Both must exist and be correct before the model is written.
Output: Updated render.go + align.go with tests; cmd/alturd/main.go patched to the new Render arity so the module keeps compiling.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tui-application/03-PATTERNS.md
@.planning/phases/03-tui-application/03-RESEARCH.md
@internal/diff/align.go
@internal/diff/render.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mode parameter to diff.Render and fix all call sites</name>
  <files>internal/diff/render.go, internal/diff/render_test.go, cmd/alturd/main.go</files>
  <read_first>
    - internal/diff/render.go (current 2-arg Render at line 60; the internal Align(file, FullFile) call at line 68)
    - internal/diff/render_test.go (ALL diff.Render call sites — confirmed at lines 50, 164, 173, 174, 183; there are five, not one)
    - cmd/alturd/main.go (the render loop at lines 70-77 calls diff.Render(file, width))
    - internal/diff/model.go (RenderMode enum: FullFile, HunkOnly)
  </read_first>
  <behavior>
    - Render(file, width, FullFile) produces identical output to the current 2-arg Render(file, width)
    - Render(file, width, HunkOnly) forwards HunkOnly to Align and never panics on any of the 13 testdata fixtures
  </behavior>
  <action>
    Change the exported signature in internal/diff/render.go line 60 from Render(file *gitdiff.File, width int) []string to Render(file *gitdiff.File, width int, mode RenderMode) []string. Change the internal call at line 68 from Align(file, FullFile) to Align(file, mode). Make no other change to render.go — the package-level dmp, ANSI constants, and per-line reset logic are unchanged.

    Update every diff.Render call site in internal/diff/render_test.go to pass an explicit mode as the third argument. Use diff.FullFile at every existing site (lines 50, 164, 173, 174, 183) since those tests assert the current full-file output. The renderFile helper at line 50 is the primary site; the width-sweep and stability assertions at lines 164/173/174/183 must each gain the diff.FullFile third argument.

    Update the single diff.Render call in cmd/alturd/main.go (inside the render loop around line 71) to diff.Render(file, width, diff.FullFile). This is a minimal compile-keeping change only — Plan 05 replaces this entire loop with the bubbletea launch. Do NOT otherwise modify main.go in this plan.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/diff/... -run TestRender</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func Render(file \*gitdiff.File, width int, mode RenderMode) \[\]string' internal/diff/render.go` succeeds
    - `grep -q 'Align(file, mode)' internal/diff/render.go` succeeds
    - No remaining 2-arg diff.Render call exists: `grep -rn 'diff.Render(' cmd internal | grep -v 'FullFile\|HunkOnly\|, mode)'` returns no lines
    - `go build ./...` exits 0 (whole module compiles despite signature change)
    - `go test ./internal/diff/... -run TestRender` passes
  </acceptance_criteria>
  <done>Render is a 3-arg function taking RenderMode; all render_test.go and main.go call sites pass an explicit mode; go build ./... is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add HunkStartRows and countFragmentRows to align.go with tests</name>
  <files>internal/diff/align.go, internal/diff/align_test.go</files>
  <read_first>
    - internal/diff/align.go (alignText at lines 165-233 — the authoritative row-accounting logic HunkStartRows must mirror; isModeOnly line 28, isSubmodule line 17, FileStatus line 38; gitdiff already imported line 8)
    - internal/diff/align_test.go (existing TestAlign table style + parseFirst helper at lines 15-30)
    - .planning/phases/03-tui-application/03-PATTERNS.md (HunkStartRows / countFragmentRows reference implementation)
    - internal/diff/testdata/ (fixtures available: simple.diff, multi-hunk.diff, binary.diff, mode-only.diff, submodule.diff, multi-file.diff)
  </read_first>
  <behavior>
    - HunkStartRows(simple.diff, FullFile)[0] == 0 and len >= 1
    - HunkStartRows(multi-hunk.diff, FullFile) returns strictly ascending indices
    - HunkStartRows(binary.diff, FullFile) == [0] (exactly one entry)
    - len(HunkStartRows(f, HunkOnly)) == len(HunkStartRows(f, FullFile)) for multi-hunk.diff (fragment count is mode-independent)
  </behavior>
  <action>
    Append two functions to internal/diff/align.go after alignText (after line 233). HunkStartRows(file *gitdiff.File, mode RenderMode) []int returns []int{0} when file.IsBinary || isModeOnly(file) || isSubmodule(file); otherwise it walks file.TextFragments, appending the running row index before each fragment and advancing the index by countFragmentRows(frag, mode). countFragmentRows(frag *gitdiff.TextFragment, mode RenderMode) int must mirror alignText's accounting exactly: OpContext lines count 1 row (in both FullFile and HunkOnly, matching alignText line 175); a run of consecutive OpDelete lines followed by consecutive OpAdd lines counts max(dels, adds) rows (matching the positional pairing plus leftovers at lines 197-220); a standalone OpAdd counts 1 row. Advance the loop index identically to alignText so the two never drift. No new imports are needed (gitdiff is already imported).

    Add TestHunkStartRows to internal/diff/align_test.go following the existing TestAlign subtest style and reusing parseFirst. Include subtests: simple_diff_one_fragment_at_row_zero (rows[0]==0, len>=1), multi_hunk_ascending_rows (each rows[i] > rows[i-1]), binary_always_row_zero (result is exactly []int{0}), and hunkonly_fragment_count_matches_fullfile (len equal for HunkOnly vs FullFile on multi-hunk.diff).
  </action>
  <verify>
    <automated>go test ./internal/diff/... -run 'TestHunkStartRows|TestAlign'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func HunkStartRows(file \*gitdiff.File, mode RenderMode) \[\]int' internal/diff/align.go` succeeds
    - `grep -q 'func countFragmentRows(frag \*gitdiff.TextFragment, mode RenderMode) int' internal/diff/align.go` succeeds
    - `go test ./internal/diff/... -run TestHunkStartRows` passes with all four subtests
    - `go test ./internal/diff/...` (full diff suite) passes
  </acceptance_criteria>
  <done>HunkStartRows returns correct ascending indices for text diffs and [0] for edge cases; countFragmentRows mirrors alignText; TestHunkStartRows is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git subprocess → diff parser | Untrusted repo content already crosses here; mitigated in Phase 1/2 (typed gitdiff structs, no shell). No new boundary added by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | HunkStartRows row indices consumed by viewport.SetYOffset | low | mitigate | countFragmentRows mirrors alignText exactly (verified by ascending + count tests); the model (Plan 03) clamps targetOffset to >= 0, so a malformed index cannot produce a negative offset or panic |
| T-03-02 | Denial of Service | Very large TextFragment count | low | accept | Row counting is O(lines), bounded by the same diff size the parser already handled in Phase 1; DIFF-04 guards apply upstream |
</threat_model>

<verification>
- `go build ./...` compiles the whole module after the Render arity change
- `go test ./internal/diff/...` passes (existing TestRender/TestAlign + new TestHunkStartRows)
- No 2-arg diff.Render call remains anywhere in cmd/ or internal/
</verification>

<success_criteria>
- diff.Render is a 3-arg function taking RenderMode; DIFF-06 toggle is now expressible
- diff.HunkStartRows exists, returns ascending indices for text diffs and [0] for edge cases; NAV-01 hunk navigation has its data source
- Module compiles; diff test suite green
</success_criteria>

## Artifacts this phase produces

- `internal/diff/render.go` — `Render(file *gitdiff.File, width int, mode RenderMode) []string` (signature changed to 3-arg)
- `internal/diff/align.go` — `func HunkStartRows(file *gitdiff.File, mode RenderMode) []int`
- `internal/diff/align.go` — `func countFragmentRows(frag *gitdiff.TextFragment, mode RenderMode) int`
- `internal/diff/align_test.go` — `TestHunkStartRows`
- `cmd/alturd/main.go` — interim 3-arg Render call (replaced entirely in Plan 05)

<output>
Create `.planning/phases/03-tui-application/03-01-SUMMARY.md` when done
</output>
